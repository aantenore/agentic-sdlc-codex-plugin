Commit: 106ab26ae12507bf54565bd1fd234109b5829e03
Author: Antonio Antenore <ant_ant95@hotmail.it>
Committed at: 2026-07-17T22:37:15+02:00
Subject: perf(observatory): bound canonical cache validation

Scope:
- validates retained canonical revisions concurrently without rebuilding unchanged models;
- reuses verified file digests through structured stat signatures while preserving canonical revision bytes;
- compares ordinary directory snapshots directly and falls back to the legacy fixed digest under an aggregate slot and name-byte budget;
- releases invalid and intermediate snapshots before subsequent scans, preserving bounded retained memory;
- keeps symlink, directory identity, readability, race, and knowledge-base boundary checks fail-closed.

Verification:
- model-cache and server tests passed 44/44 on Node.js 24 and Node.js 18.20.8;
- wide, long-name, add, remove, rename, type-change, metadata-only, unreadable, symlink, and scan-race cases passed;
- canonical revision and observable directory digest golden values remained unchanged;
- final independent review found no P0-P2 issues after two bounded-memory findings were fixed;
- five final full-scale runs stayed within all latency and RSS budgets.
