ST-ENT-PERFORMANCE Windows CI portability repair
Recorded at: 2026-07-17T23:10:51Z

Outcome: local validation passed; Windows matrix verification pending CI

The cache signature no longer relies on ctime, which is not a portable stable
cache key on Windows. File identity, type, permissions, size, modification time,
canonical content digests, and bounded directory snapshots remain enforced.

The Observatory worker now gives the operating system's close event the existing
bounded termination window to report the real exit code and stderr after an IPC
disconnect. Platform-specific skipped tests return immediately instead of
continuing into unsupported permission or symlink assertions.

Validation:
- npm run check: passed
- npm test: passed
- Node.js 18.18 targeted cache and benchmark tests: 74/74 passed
- Current Node.js targeted cache and benchmark tests: 74/74 passed
- Strict ST-ENT-PERFORMANCE story gate: passed
- Git diff check: passed

Remote acceptance remains the GitHub Actions Windows matrix on Node.js 18.18,
20, and 24. A new push also triggers a fresh GitGuardian check after the two
non-secret values were classified as false positives in GitGuardian.
